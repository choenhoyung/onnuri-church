# 청주 온누리감리교회 웹사이트 개발 작업내역

- **배포 주소**: https://onnuri-church-nu.vercel.app/
- **관리자 페이지**: https://onnuri-church-nu.vercel.app/admin/
- **GitHub 저장소**: https://github.com/choenhoyung/onnuri-church
- **기술 스택**: 순수 HTML/CSS/JavaScript(바닐라, 프레임워크 없음) + Supabase(DB·인증·스토리지) + Vercel(호스팅) + Supabase CLI(마이그레이션 자동화)

---

## 1. 프로젝트 개요

교회 자체 홈페이지가 없어서 처음부터 새로 제작. 정적 사이트로 시작해 Supabase를 백엔드로 붙여 **관리자가 코딩 없이 모든 콘텐츠를 직접 수정할 수 있는 CMS형 홈페이지**로 발전시킴. 이후 신반포교회, 사랑의교회, 여의도순복음교회, 엘파소 한인 순복음교회 등 여러 교회 사이트의 UI/UX 구조를 참고하여 디자인을 고도화함(단, 실명·실제 콘텐츠는 복제하지 않고 구조/패턴만 참고, 데이터는 온누리교회 실제 정보 사용).

---

## 2. 초기 설정

- Supabase 프로젝트 연결 (URL + publishable key)
- `.env` 파일 관리
- GitHub 저장소 생성 (`onnuri-church`) 및 Vercel 자동 배포 연동 (push 시 자동 반영)
- **Supabase CLI 자동화 파이프라인 구축**: `supabase login` (OAuth, 비밀번호 채팅 노출 없이) → `supabase link` → 이후 모든 DB 스키마 변경은 마이그레이션 파일(`supabase/migrations/*.sql`) 작성 후 `supabase db push` 한 번으로 반영. 대시보드 SQL Editor에 매번 복붙할 필요 없음.

---

## 3. 공개 페이지 (Public Pages)

| 파일 | 설명 |
| --- | --- |
| `index.html` | 홈페이지 — 히어로(배경사진/그라데이션 선택 가능), 예배시간 카드, 퀵메뉴, 담임목사, 최근 설교, 교회소개 요약, 오시는 길(카카오맵), 우측 스크롤 탭 |
| `gallery.html` | 앨범 — 부서(전체/유치부/초등부/학생부/청년부/장년부) 탭 + 연도 드롭다운 필터 |
| `bulletins.html` | 교회주보 — 목록 + 페이지 내 인라인 PDF/이미지 뷰어(펼치기/접기) |
| `department.html?slug=` | 부서별 독립 소개 페이지 (소개글, 담당자, 배너사진 + 해당 부서 앨범) |
| `schedule.html` | 교회 일정 — 월간 캘린더(이전/다음달 이동, 오늘/일정 표시) + 선택일 일정 패널 |
| `about.html` | 교회소개 — 탭 전환형: 담임목사 인사 / 비전과 사명 / 교회 연혁(타임라인) / 섬김의 사람들(카드) |
| `admin/index.html` | 관리자 페이지 (로그인 필요) |
| `index-v1.html` | 초기 버전 홈페이지 아카이브 (비교용, `styles-v1.css`/`menu-v1.js` 고정본 사용) |
| `templates/` | 탐색용 UI 템플릿 3종 (모던/커뮤니티/아트풍, 실제 서비스에는 미적용) |

### 공통 컴포넌트
- **메가메뉴**: 관리자가 편집한 메뉴 데이터 기반 데스크톱 드롭다운 GNB
- **전체 사이트맵 모달**: 햄버거 클릭 시 전체 메뉴를 4단 그리드로
- **모바일 드로어**: 아코디언식 전체 메뉴
- **모바일 하단 고정 네비(Bottom Nav)**: 예배/설교/앨범/주보/더보기 5개 아이콘, 현재 페이지 하이라이트
- **팝업 공지**: 좌상단 이미지 팝업, "닫기"/"오늘 하루 보지 않기"(localStorage 자정까지)
- **스크롤 리빌 애니메이션**: 카드가 스크롤 시 페이드인 (모션 최소화 설정 존중)
- **SEO/OG 메타태그, 파비콘** 전 페이지 적용

---

## 4. 관리자 페이지 (admin/index.html)

사이드바 레이아웃, 로그인 기반(Supabase Auth), 아래 탭들로 구성:

| 탭 | 기능 |
| --- | --- |
| 홈페이지 문구 | 히어로 제목/표어, 담임목사 이름·소개, 주소/전화, 설립일, WORSHIP/LOVE/BIBLE 소개글, 예배시간 목록(추가/수정/삭제/순서) |
| 앨범 | 사진 여러 장 동시 업로드(자동 압축), 부서/연도 태그, 설명 수정, 삭제 |
| 주보 게시판 | 개별 등록(제목/날짜/설명/파일) + **여러 장 한번에 업로드(OCR 자동인식)**: 사진에서 날짜·본문 구절 자동 추출 → 편집 가능한 표로 확인 후 일괄 등록 |
| 메뉴 관리 | GNB/사이트맵/모바일메뉴에 공통 반영되는 메뉴·서브메뉴 추가·삭제·순서변경 |
| 사이트 꾸미기 | 배너 배경색/글자색, 사이트 글꼴(5종), 헤더/히어로 스타일(기본/모던/커뮤니티/아트풍), 히어로 배경사진, 담임목사 프로필 사진, 카카오맵 JS 키 |
| 설교 영상 | 유튜브 API 키 + 채널 URL 저장, "지금 동기화"로 최신 영상 자동 수집(중복 방지) → 홈페이지 "최근 설교" 섹션에 반영 |
| 팝업 공지 | 팝업 이미지/링크/활성화 여부 |
| 부서 소개 | 부서별 소개글/담당자/배너사진 편집 |
| 교회 일정 | 일정 추가/수정/삭제(제목/날짜/시간/장소/설명) |
| 교회소개 관리 | 비전과 사명 문구, 교회 연혁(연도별), 섬김의 사람들(이름/역할/사진) |

**보안 설계**: 관리자 전용 데이터(유튜브 API 키 등)는 로그인한 사용자만 읽을 수 있는 테이블에 저장, 일반 방문자에게 노출 안 됨. 모든 쓰기 작업은 Supabase RLS(Row Level Security)로 로그인 세션 없이는 불가능하도록 차단.

---

## 5. 주요 트러블슈팅 기록

| 문제 | 원인 | 해결 |
| --- | --- | --- |
| 사진 업로드 실패 | 한글/공백 포함 파일명을 Storage 경로로 사용 | 타임스탬프+랜덤 문자열로 안전한 파일명 생성 |
| 관리자 탭 이동 시 위쪽 여백 과다 | 탭 전환 시 스크롤 위치 미초기화 | 탭 클릭 시 `scrollTo(0,0)` 추가 |
| 관리자 대시보드 레이아웃 깨짐(사이드바 아래로 밀림) | JS가 `display:block`으로 설정(사이드바 레이아웃은 `flex` 필요) | `display:flex`로 수정 |
| 실제 크롬에서 페이지 전체 클릭 안 됨 | 숨겨진 사이트맵 모달이 `display:flex`로 고정되어 투명한 채로 전체 화면을 덮고 있었음 (`[hidden]` 속성보다 클래스 규칙이 우선 적용됨) | `.sitemap-modal[hidden] { display:none }` 규칙 추가 |
| 색상 피커가 얇은 선처럼 보임 | 공용 input 패딩이 `input[type=color]` 스와치를 짓눌러버림 | 색상 입력창 전용 padding/height 지정 |
| 구글맵 → 네이버맵 교체 시 지도가 안 뜸 | 네이버 지도는 `X-Frame-Options: DENY`로 iframe 삽입 자체를 차단 | 클릭 시 네이버지도로 이동하는 링크 카드로 대체 |
| 카카오맵 연동 후에도 안 뜸 | 카카오 개발자 콘솔에서 앱에 "카카오맵" 제품이 비활성화 상태 | 사용자가 콘솔에서 카카오맵 서비스 활성화 후 해결 |
| 메뉴 관리에 새 메뉴 추가해도 상단에 안 뜸 | "교회 일정/교회소개 관리" 탭(콘텐츠 입력)과 "메뉴 관리" 탭(네비게이션 링크)을 혼동 — 메뉴 관리에서 별도로 링크 추가 필요 | 절차 재안내 |

---

## 6. 데이터베이스 구조 (Supabase, `public` 스키마)

| 테이블 | 용도 |
| --- | --- |
| `site_settings` | 홈페이지 문구, 배너/폰트/히어로스타일, 히어로배경사진, 담임목사 사진, 카카오맵 키, 비전 문구 (단일 행) |
| `worship_times` | 예배시간 목록 |
| `photos` | 앨범 사진 (부서/연도 태그) |
| `bulletins` | 교회주보 (제목/날짜/설명/파일) |
| `site_menu` | GNB·사이트맵·모바일메뉴 데이터 (JSONB) |
| `sermons` | 유튜브 동기화된 설교 영상 목록 |
| `integration_settings` | 유튜브 API 키/채널 (관리자만 읽기 가능) |
| `popup_settings` | 팝업 공지 이미지/링크/활성화 |
| `departments` | 부서별 소개(슬러그/이름/설명/담당자/배너) |
| `events` | 교회 일정 |
| `church_history` | 교회 연혁 |
| `serving_team` | 섬김의 사람들 |

Storage 버킷: `gallery-photos`, `bulletin-files`, `hero-images`, `popup-images` (모두 공개 읽기, 관리자만 쓰기)

---

## 7. 남은 작업 / 사용자가 직접 해야 할 일

- [ ] 관리자 → **메뉴 관리**에서 "교회소개"(`about.html`), "교회 일정"(`schedule.html`) 메뉴 항목 추가 후 **전체 저장** (콘텐츠는 이미 입력했지만 메뉴 연결 안 됨)
- [ ] 교회 연혁, 섬김의 사람들, 교회 일정에 입력한 테스트 데이터("ㅇㅇㅇ" 등)를 실제 정보로 교체
- [ ] 담임목사 실사 프로필 사진 업로드 (사이트 꾸미기 탭)
- [ ] "최근 설교"의 테스트 영상 삭제 후 실제 유튜브 채널로 재동기화
- [ ] 카카오맵: 개발자 콘솔에서 "카카오맵" 서비스 활성화 여부 최종 확인
- [ ] 헌금계좌 등 민감정보는 사이트에 넣지 않기로 결정된 상태 (필요 시 재논의)

---

*이 문서는 대화 세션 전체 작업을 정리한 것으로, 이후 추가 작업은 별도로 갱신이 필요합니다.*
